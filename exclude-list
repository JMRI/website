# List of files to exclude when rsync'ing this directory
# to a website.  This allows exclusion of every other
# file from delete, e.g. in other repositories
.git
.gitignore
include-list
exclude-list 
